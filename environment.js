
const path = require('path')
var { config } = { config: {} };

config = {

    domain: 'bs4-breakpoint.example.domain.com',
    pwa: {
      name: 'Bootstrap 4 breakpoints detector',
      short_name: 'bs4-breakpoint',
    }
}
module.exports = exports = { config };
